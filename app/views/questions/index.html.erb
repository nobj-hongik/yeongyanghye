<div class="container-fluid">
  <div class="row">
    <div class="col-xs-0 col-md-1 col-lg-2 col-xl-3"></div>
    <div class="col-xs-12 col-md-10 col-lg-8 col-xl-6">
      <h2 class="title_size slim-font mg-top-med">영양해
        <span class="bold-font orange">영양in</span>
        <a href="/questions/new" class="small_compact_btn hvr-right float-right">질문하기</a>
      </h2>
      <p class="semi_slim-font mg-bottom-med luxury_space info-size">
        세상의 모든 영양 지식, 영양해에게 물어봐!
      </p>
      <% @questions.each do |question| %>
        <a href="/questions/<%=question.id%>">
          <div class="question_div">
          <span class="cate_wrap"><%= question.category %></span>
          <% if question.answer_already_selected %>
            <span class="cate_wrap green_back">채택 완료!</span>
          <% else %>
            <span class="cate_wrap orange_back">채택 대기중</span>
          <% end %>
          <p class="luxury_space mg-top-sm semi_bold-font sub_size"><%= question.title %></p>
          <% if question.owner_selected? %>
            <p class="slim-font info-size">채택이 완료된 질문이에요</p>
          <% else %>
            <p class="slim-font info-size"><%= question.question_likes.size%>명이 답변을 기다리고 있어요</p>
          <% end %>
          </div>
        </a>
      <% end %>
    </div>
    <div class="col-xs-0 col-md-1 col-lg-2 col-xl-3"></div>
  </div>
</div>
